# 粗視化・条件間転移の研究評価と、A 固定 pilot

対象は今回の SimVP／粗視化／E・B 条件間転移の研究主張である。
別系統で行ってきた PIC の履歴・緩和・独立 realization 解析まで一括して格付けするものではない。

## 現時点の率直な評価

**続ける価値はある。ただし「二つの課題に共通する物理機構が見えた」という主張は、現状では未成立。**
主観的な目安を求めるなら、現時点の検証済み成果の強さは **4–5/10**、研究の問いの潜在価値は **7–8/10**。
これは採択確率や客観的スコアではない。前者は現在論文で支えられる主張、後者は追加検証が成功した場合の価値を区別している。
以前の「結果によってはかなり面白い」は潜在価値の評価としては残るが、現時点の証拠の強さと混同してはいけない。

今あるのは、実装交絡を減らした上での、条件に依存する輸送予測の失敗と改善余地である。
「BN修正で良くなった」「MSEが小さくてもスペクトルや輸送を外す」「physics lossを足すと良くなる」だけでは、
強い新規性を持つ研究の中心としては不足する。問いを具体化する必要がある。

プラズマ乱流 surrogate でスペクトルだけでなく輸送を検証する先行例は、Hasegawa–Wakatani系を対象とした
[GAIT, Clavier et al.](https://arxiv.org/abs/2405.13232) にある。
また、L2学習による乱流構造の過平滑化、超解像・予測に対する生成モデルの改善は
[Oommen et al., Nature Communications (2026)](https://www.nature.com/articles/s41467-026-70145-4) で扱われている。
両論文は今回のHall型kinetic PIC、符号付き密度–電場相関、native coarseとE/B転移の組合せをそのまま検証したものではない。
この違いを実験で示す余地はあるが、この確認だけで先行例がないと主張することもできない。

## 今回追加した基準モデル比較

解析コード：`analyze_radaz_nextstep_baselines.py`

結果：`workdirs/2D_RadAz/radaz_nextstep_baselines_v1/results.json`

ARは条件別・観測成分別のAR(10)で、10時点の観測履歴から10時点先まで直接予測する。
fitはsource-train後半1200–1599、正則化候補は事前固定しsource-validationで選択した。
modal fluxのridgeは1e-4、全mode流束は1e-2。その値をsource-testへ固定適用した。
同じ10個の非重複10→10窓を、copy、入力平均、train平均、線形外挿、AR、旧A last/inputで比較した。
ARは各条件の流束履歴を直接使う。条件間zero-shot手法でも、場全体のsurrogateでもない。
SimVPはtrain全体0–1599を使うため、学習データ量も完全一致していない。

source-testでのskillは `1 − SSE(method) / SSE(copy)`。正ならcopyより良い。

| 条件 | modal flux：旧A | modal flux：AR | 全mode流束：旧A | 全mode流束：AR |
|---|---:|---:|---:|---:|
| E10_B20 | +0.483 | +0.333 | +0.566 | +0.204 |
| E20_B20 | +0.734 | +0.596 | +0.739 | +0.606 |
| E30_B20 | +0.648 | +0.122 | +0.693 | −0.149 |
| E40_B20 | −0.038 | +0.952 | +0.026 | +0.953 |
| E10_B10 | −0.377 | +0.250 | −0.351 | +0.206 |
| E10_B30 | −0.757 | −0.002 | −3.514 | +0.290 |

![Baseline skill](workdirs/2D_RadAz/radaz_nextstep_baselines_v1/baseline_skill.png)

E10/20/30_B20では旧AがARより良い。E40_B20とB10/B30ではARが旧Aを上回る。
一つの「予測限界」「高周波を表現できない」で全条件を説明する証拠にはならない。
B30ではmodal fluxをcopy以上にする難しさが残る一方、全mode流束はARでも改善する。
観測量をどの程度集約するかも結論を変える。

E40_B20のtrainでの流束変動rmsは全体rmsの約1.6%で、copyの誤差自体が小さい。
従ってARの約0.95というskillを「大きな輸送量を回復した」とは読まない。
同条件のACFの最初の1/e crossingは15nsだがARは150nsまでの窓で良いため、
短いACF crossingをそのまま予測不能時間とは扱えない。

## 残差から残っている面白さ

局所積・input BNで評価し直しても、場や複素係数の高いcopy skillと輸送の低いskillが併存する。
輸送を重視した表現・損失・読み出しを検証する動機は残る。

旧AのB30 modal誤差上位は径方向band 1と4のn=4で、合計約50%のSSEを占める。
B10でもn=2,4が上位にある。少なくとも今回の残差を高modeだけの不足として扱う根拠は弱い。
B30の平均biasが占めるSSEはmodalで約24%、全mode流束で約41%。平均のずれだけで全誤差は消えない。

`Γ = −2 A O / B`, `A = sqrt(Pn Pe)`, `O = Re(C)/A`として、
予測誤差を振幅項・O項・交互作用項に厳密に分解した。
B10/B30のO項単独の二乗ノルム／総SSEは約0.77/0.86だが、
交差項があるため「誤差の77%/86%を因果的に説明した」という寄与率ではない。
Oはband内の規格化した相互スペクトルであり、単一波の位相角だけを表すとも限らない。

## 粗視化とE/B転移の共通性について

共通仮説は「個々の場に対する誤差だけでは、場どうしの関係が決める電荷・輸送を保証できない」と置くのが妥当。
ただし電荷 `ρ=e(ni−ne)` は線形、輸送は密度と電場の積であり、同じ問題として証明済みではない。

粗視化側の既存結果には、人工downsampleとnative coarseの分布差、
Poisson整合を改善してもmode powerを失うlifting、位相整列による改善という手掛かりがある。
ただし以下を残している。

1. fine-origin rollout自体の不安定性を、今回と同様に実装・正規化・copy対照で切り分ける。
2. artificial coarseとnative coarseを分け、PIC seed、数値解像度、粒子数・noiseの差を確認する。
3. 位相混合のpower減衰に対するR²という関係は同じ係数から導かれる代数的関係でもある。
   独立な予測則としての価値は、未使用データ上の予測と事前に決めた位相介入で示す。
4. 異なるnative/fine realizationに対する一対一の場MSEは、初期条件・位相の対応がなければ
   適切な目標にならない可能性がある。対応を揃えるか、輸送・スペクトル・条件付き分布を評価する。

さらにmemoの「個々の密度が数%誤っているので、電荷を原理的に予測できない」という断定を修正した。
`||εi−εe||² = ||εi||² + ||εe||² − 2〈εi,εe〉`であり、誤差の共分散が必要である。
これはむしろ「各場の誤差だけでなく、場間の誤差構造を測る」という研究課題を明確にする。

同様に条件付き平均からの輸送なら `E[nEy|h] = E[n|h]E[Ey|h] + Cov(n,Ey|h)`。
この恒等式自体は新発見ではない。今回の残差が条件付き不確定性によるのか、
決定論的モデルの誤学習によるのかは、今回の一realizationの予測誤差だけでは判別できない。

## 進めた実験と、その結果で決める次の一手

固定計画：`RADAZ_PAIRED_PILOT_PROTOCOL.md`

有効な実行bundle：`workdirs/2D_RadAz/radaz_paired_pilot_v2/bundle.json`

- D：reduced A + GroupNorm、field MSE。
- P：Dと同じ構造・初期重み、field MSE + 修正済みlocal power/cross-spectrum loss。
- seed42、各60 epoch、主checkpointはlast index59。D→Pの後に両者を同じ評価器で評価する。
- Pの重みは既存source-train calibration値を固定。途中のDや既知source-testで調整しない。
- 主に時刻別の全mode流束とmodal fluxをcopy/ARと比べ、場・符号付きO・lead・worst条件も報告する。

v1はsourceのtest所属欠落によるローダーエラーで重み更新前に停止した。
v2は所属指定を修正し、同一の学習configを使用する。v1のbundle・コード・ログを保存した。
18件の関連unit test（既存11 + baseline3 + pilot4）が通過した。
実データのD/P forward/backwardでは初期予測の完全一致、有限勾配、train/eval一致を確認した。
これは60 epochの成功結果ではない。進捗・失敗・完了はv2の`status.json`と`train_D.log`/`train_P.log`を確認する。

| 結果の形 | 次の一手と主張の限界 |
|---|---|
| PがDとARを上回り、悪い条件も改善する | 両条件をseed43/44でも反復。独立PICに固定適用し、介入効果の再現を確認する |
| D/Pとも改善し、両者差が小さい | 旧Aからの改善を物理lossの成果にしない。正規化等の影響を評価し、粗視化側の再監査へ |
| Pが平均スペクトルだけ改善し、時刻別輸送は改善しない | 現lossの窓内平均という制約に対応。時刻別相互スペクトル／流束lossの対照を次の一つに選ぶ |
| D/PともARを下回る | 流束の直接予測や読み出し・履歴を優先して検証。構造を大量に拡大する根拠にはしない |

Pは窓内平均の統計lossなので、陰性結果は「物理loss一般が効かない」ことを意味しない。
seed42で有意性や普遍性を主張せず、既知source-testを独立確認用に呼び替えない。
AR対照は特定観測量の予測可能性を調べるもので、場surrogateの汎化能力を全面的に代替するものでもない。

独立PICの初期候補はE20_B20とE10_B30で、既存Aの良好／不良ケースを含める。
同じ条件の独立seedは再現性の試験であり、未見運転条件への一般化試験とは分ける。
粗視化・E/B転移の共通性を強く主張する段階では、未使用条件、native coarse、時間窓をあらかじめ固定し、
sourceで決めた診断が失敗の場所と介入効果を予測することが必要になる。

研究の中心は「粗視化でもE/B転移でも似た失敗が起きた」から、
**どの場間相関の誤差がどの物理量を壊し、それをどの観測・介入で予測して防げるか**へ具体化する。
そこまで届けば、今回の修正後も十分に面白い研究になる可能性がある。
